 <!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<title>Rocket Elevator - An elevator to Heaven</title>
		<meta name="description" content="" />
		<meta name="Author" content="Team poulet" />
		<meta name="viewport" content="width=device-width, maximum-scale=1, initial-scale=1, user-scalable=0" />

		<!-- WEB FONTS : use %7C instead of | (pipe) -->
		<link href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600%7CRaleway:300,400,500,600,700%7CLato:300,400,400italic,600,700" rel="stylesheet" type="text/css" />
		<%= stylesheet_link_tag "application" %>
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">

		<script src="<%= asset_path('rocketElevator/jquery-3.3.1.min') %>"></script>
		<%= yield(:head) %>
	</head>

    <body>
     <%= render 'home/header' %>
      <% if notice %>
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
          <strong><%= notice %></strong>
          <button id="close" type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">X</button>
        </div>
        <% elsif alert %>
          <p class="alert"><%= alert %></p>
      <%end%>

        <!-- I render the header layer by using the render option -->
       
        <!-- yield is the file which our controller will use for the inner part of the application -->
        
        <%= yield %>
        <%= render 'home/footer' %>

        <script>
          document.getElementById('close').addEventListener('click', e => {
          e.target.parentElement.style.display = 'none';
           });
       </script>
    </body>
</html>

 
